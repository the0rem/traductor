[Unit]
Description={{ service.description }}
After=docker.service
Requires=docker.service

[Service]
ExecStartPre=/usr/bin/docker pull {{ service.image }}
ExecStart=/opt/bin/systemd-docker --cgroups name=systemd run {{ service.run_params }} --rm --name %n {{ service.name }}
Restart=always
RestartSec=10s
Type=notify
NotifyAccess=all
TimeoutStartSec=120
TimeoutStopSec=15

[Install]
WantedBy=multi-user.target
